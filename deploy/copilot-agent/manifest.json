{
  "$schema": "https://developer.microsoft.com/json-schemas/teams/v1.19/MicrosoftTeams.schema.json",
  "manifestVersion": "1.19",
  "version": "{{PACKAGE_VERSION}}",
  "id": "{{APP_GUID}}",
  "developer": {
    "name": "Trakt",
    "websiteUrl": "{{API_BASE_URL}}",
    "privacyUrl": "{{API_BASE_URL}}",
    "termsOfUseUrl": "{{API_BASE_URL}}"
  },
  "name": {
    "short": "Trakt",
    "full": "Trakt Portfolio MI Agent"
  },
  "description": {
    "short": "Portfolio MI answers and latest Trakt artefacts in Copilot.",
    "full": "Ask Trakt portfolio MI questions and retrieve the latest generated investor deck or canonical loan tape. Answers come from Trakt's deterministic MI engine — never model estimation. v1 supports only these three capabilities."
  },
  "icons": {
    "color": "color.png",
    "outline": "outline.png"
  },
  "accentColor": "#1F3A5F",
  "copilotAgents": {
    "declarativeAgents": [
      {
        "id": "traktDeclarativeAgent",
        "file": "declarativeAgent.json"
      }
    ]
  },
  "validDomains": [
    "{{API_HOST}}"
  ]
}
