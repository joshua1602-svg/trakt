# -----------------------------------------------------------------------------
# Trakt landing page — environment
#
# Copy to `.env.local` for development. In Azure these become App Service
# application settings (or Container Apps environment variables / secrets).
# Nothing here is committed with a real value.
# -----------------------------------------------------------------------------

# --- Site ---------------------------------------------------------------------
# Canonical public origin. Drives metadataBase, canonical URL, Open Graph and
# sitemap. Set it to whichever hostname you publish under — the code never
# hard-codes a domain.
#   e.g. https://trakt.<your-domain>  |  https://www.<your-domain>
#        https://demo.<your-domain>
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Display name of the synthetic demonstration client.
NEXT_PUBLIC_DEMO_NAME=Synthetic Demo Lender

# development | staging | production. Production turns on secure cookies and
# makes DEMO_SESSION_SECRET mandatory.
APPLICATION_ENV=development

# --- Demo API -----------------------------------------------------------------
# Reserved. Unset today: the demo is served from the committed demo pack, in
# process. Set only if a future deployment proxies a separate private service.
# DEMO_API_BASE_URL=

# Signs the demo-session cookie (question/report counters). REQUIRED in
# production; a random ephemeral key is used in development.
# Generate: openssl rand -hex 32
DEMO_SESSION_SECRET=

# --- Demo session limits ------------------------------------------------------
DEMO_MAX_QUESTIONS_PER_SESSION=12
DEMO_MAX_REPORTS_PER_SESSION=3
DEMO_SESSION_TTL_SECONDS=7200
DEMO_MAX_QUESTION_LENGTH=240
DEMO_MAX_BODY_BYTES=4096

# --- Rate limits (per client IP) ----------------------------------------------
RATE_LIMIT_DEMO_PER_MINUTE=30
RATE_LIMIT_REPORT_PER_MINUTE=10
RATE_LIMIT_LEAD_PER_HOUR=5

# Optional shared counter store for a multi-instance deployment. Leave unset for
# the single-instance in-process store (see README § Known limitations).
# RATE_LIMIT_STORE_URL=

# --- Lead delivery ------------------------------------------------------------
# file | email | webhook | console. Exactly one is active.
#   file    — development-safe default; appends to LEAD_STORE_DIR/leads.jsonl
#   email   — transactional email provider (Resend-compatible JSON API)
#   webhook — POST to a CRM/automation endpoint, optionally HMAC-signed
#   console — structured log line only (used by the test suites)
LEAD_DELIVERY_PROVIDER=file
LEAD_STORE_DIR=.leads

# LEAD_DELIVERY_PROVIDER=email
# EMAIL_API_KEY=
# EMAIL_API_URL=https://api.resend.com/emails
# LEAD_FROM_EMAIL=trakt-website@<your-domain>
# LEAD_NOTIFICATION_EMAIL=sales@<your-domain>

# LEAD_DELIVERY_PROVIDER=webhook
# CRM_WEBHOOK_URL=
# CRM_WEBHOOK_SECRET=

# Minimum milliseconds a form must be on screen before submission is accepted.
LEAD_MIN_FILL_MS=2500

# --- Analytics ----------------------------------------------------------------
# none | appinsights | firstparty. Default `none` records nothing anywhere.
NEXT_PUBLIC_ANALYTICS_PROVIDER=none
# APPLICATIONINSIGHTS_CONNECTION_STRING=

# --- Demo video ---------------------------------------------------------------
# Absolute https URL, or a path under public/. Unset renders the documented
# placeholder and the page still works.
# NEXT_PUBLIC_DEMO_VIDEO_URL=
# NEXT_PUBLIC_DEMO_VIDEO_POSTER=

# --- Misc ---------------------------------------------------------------------
# NEXT_PUBLIC_CONTACT_EMAIL=
# PORT=3000
