pandas>=1.5.0,<3.0.0
numpy>=1.23
matplotlib>=3.5
python-pptx>=0.6.21
openpyxl>=3.0
streamlit>=1.24
plotly>=5.14
PyYAML>=6.0
lxml>=4.9
rapidfuzz>=3.0
scikit-learn>=1.2
Jinja2>=3.1
anthropic>=0.40.0
azure-functions>=1.17
azure-storage-blob>=12.19
azure-identity>=1.15

# --- MI Agent API server (trakt-mi-api Azure App Service) ---
# Installed by Oryx for the App Service code deploy; unused by the Function App.
fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=21.2
uvicorn-worker>=0.2
# Multipart form parsing — required by FastAPI for the Operations Control
# manual-delivery upload route (operations_control/api/app.py).
python-multipart>=0.0.9
# Entra ID bearer-token validation for the Microsoft 365 Copilot action routes
# (mi_agent_api/copilot_auth.py). [crypto] pulls the RS256 signature backend.
PyJWT[crypto]>=2.8

# --- Client-facing document rendering (trakt_mail/presentation.py) ---
# The onboarding checklist a client receives as a PDF attachment. Pure Python,
# no system libraries, so it installs under Oryx on App Service without a build
# step. Markdown remains the internal source artefact; this renders it.
reportlab>=4.0,<6
