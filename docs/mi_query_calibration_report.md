# MI `/query` Dimension-Invariant Calibration Report

_Generated by `scripts/mi_query_calibration.py` from the registry-driven golden harness (`mi_agent/mi_query_harness.py`). Deterministic and offline — no LLM/network._

## Headline

- **Cases run:** 249
- **Passed:** 249  
- **Failed:** 0  
- **Pass rate:** 100.00%
- **Unsupported concepts correctly refused:** 5

The single guarantee under test: every grouping dimension the parser recognises is either **applied** (present in the executor group columns / result columns and surfaced on the chart axes or table) or **explicitly rejected** with a reason — **never silently dropped**. A requested metric must likewise appear in the result payload rather than being substituted with the default balance.

## Fail-closed invariants (tracked separately)

| Invariant | Cases checked | Held | Breached |
|---|---:|---:|---:|
| Dimension (parsed dim applied or explicitly rejected) | 243 | 243 | 0 |
| Filter (parsed filter applied or explicitly surfaced) | 243 | 243 | 0 |

Value filters were genuinely exercised on **31** cases (parsed AND applied to the execution mask). A parsed filter that is neither applied nor explicitly surfaced fails the query closed rather than returning silently unfiltered data.

## Coverage by query class

| Class | Passed | Total | Pass rate |
|---|---:|---:|---:|
| single_dim | 72 | 72 | 100% |
| two_dim | 60 | 60 | 100% |
| three_dim | 20 | 20 | 100% |
| filtered_kpi | 7 | 7 | 100% |
| grouped_filter | 24 | 24 | 100% |
| filter_unsupported | 1 | 1 | 100% |
| top_n | 12 | 12 | 100% |
| ranking | 24 | 24 | 100% |
| weighted_avg | 12 | 12 | 100% |
| count | 12 | 12 | 100% |
| rejection | 5 | 5 | 100% |

## Failures by class

None — every generated query held the invariant.

## Dimensions exercised

Registry dimensions that both parsed and executed against the fixture (the harness discovers these — it does not hardcode them):

- `account_status`
- `age_bucket`
- `borrower_type`
- `broker_channel`
- `erm_product_type`
- `geographic_region_obligor`
- `interest_rate_type`
- `ltv_bucket`
- `occupancy_type`
- `origination_channel`
- `term_bucket`
- `ticket_bucket`

## Sample queries per class (passing)

**single_dim**
  - balance by Account Status
  - balance by status
  - valuation by Account Status
  - valuation by status

**two_dim**
  - balance by Account Status by Age Bucket
  - balance by Account Status by Borrower Type
  - balance by Account Status by Broker
  - balance by Account Status by Product Type

**three_dim**
  - balance by Account Status by Age Bucket by Borrower Type
  - balance by Account Status by Age Bucket by Broker
  - balance by Account Status by Age Bucket by Product Type
  - balance by Account Status by Age Bucket by Obligor Region (NUTS3)

**filtered_kpi**
  - how many loans have LTV above 30%
  - how many loans have LTV above 50%
  - how many loans have LTV above 60%
  - how many loans have balance above 150000

**grouped_filter**
  - balance by Account Status where LTV above 50%
  - balance by Age Bucket where LTV above 50%
  - balance by Borrower Type where LTV above 50%
  - balance by Broker where LTV above 50%

**filter_unsupported**
  - how many loans have Risk Score above 700

**top_n**
  - top 5 Account Status by balance
  - top 5 Age Bucket by balance
  - top 5 Borrower Type by balance
  - top 5 Broker by balance

**ranking**
  - which Account Status has the largest balance
  - which Account Status has the smallest balance
  - which Age Bucket has the largest balance
  - which Age Bucket has the smallest balance

**weighted_avg**
  - weighted average interest rate by Account Status
  - weighted average interest rate by Age Bucket
  - weighted average interest rate by Borrower Type
  - weighted average interest rate by Broker

**count**
  - how many loans by Account Status
  - how many loans by Age Bucket
  - how many loans by Borrower Type
  - how many loans by Broker

**rejection**
  - How many loans are in arrears?
  - Show defaulted balance by region.
  - Show NNEG exposure by LTV bucket.
  - Show credit score by broker.

## Grouped query + value filter (now supported)

Evidenced from live behaviour:

- Probe: `balance by Obligor Region (NUTS3) where LTV above 50%`
- Filter parsed into the spec: **True**
- Filter applied (reconciliation): **True** (156 of 400 records)
- Supported: **True**

A value filter combined with a grouping (`… by <dim> where <measure> above <x>`, ranges, and categorical `… for joint borrowers`) is now applied to the execution mask **before** grouping — both the grouping and the filter survive. The fail-closed **filter invariant** refuses the query if a parsed filter is not applied, so unfiltered grouped data is never returned silently. A filter on a field ABSENT from the dataset is refused or surfaced as unavailable (`filter_unsupported` class), never silently ignored.

## Correctly-rejected unsupported concepts

- How many loans are in arrears? — refused ✅
- Show defaulted balance by region. — refused ✅
- Show NNEG exposure by LTV bucket. — refused ✅
- Show credit score by broker. — refused ✅
- Show recoveries in period by region. — refused ✅

## How to reproduce

```bash
# The invariant regression (fast, offline):
python -m pytest mi_agent/tests/test_mi_query_invariants.py -q

# Regenerate this report:
python scripts/mi_query_calibration.py
```


---

# Curated calibration bank (realistic business questions)

Additional to the generated harness above: a hand-curated bank of realistic lender / investor / credit-committee / portfolio-manager / ops questions, each with declared expected semantic behaviour (`config/mi/golden_questions/ere_mi_calibration_250.yaml`, enforced by `mi_agent/tests/test_mi_calibration_bank.py`).

- **Curated questions:** 252
- **Held their declared expectation:** 236
- **Hard failures (non-known-gap):** 0
- **Known-gap cases (flagged):** 17 — of which **16** currently fail (xfailed with a reason) and **1** already meet the ideal (flag can be retired).
- **Pass rate (known gaps counted as not-passing):** 93.7%

## Pass / fail by category

| Category | Passed | Total | Known gaps | Hard fails |
|---|---:|---:|---:|---:|
| ambiguous | 6 | 10 | 4 | 0 |
| basic_kpi | 31 | 32 | 1 | 0 |
| data_quality | 5 | 9 | 4 | 0 |
| filtered | 36 | 37 | 1 | 0 |
| forecast | 9 | 9 | 0 | 0 |
| pipeline | 12 | 12 | 0 | 0 |
| ranking | 23 | 23 | 0 | 0 |
| risk | 14 | 20 | 7 | 0 |
| single_dim | 64 | 64 | 0 | 0 |
| two_dim | 26 | 26 | 0 | 0 |
| unsupported | 10 | 10 | 0 | 0 |

## Failure classes (hard failures only)

None — every non-known-gap curated question held its declared expectation (metric resolution, dimensions applied, filters applied, dimension & filter invariants, artifact type, reconciliation).

## Unsupported concepts correctly refused

- show defaulted balance by region — refused ✅
- defaulted balance — refused ✅
- show NNEG by region — refused ✅
- NNEG exposure by LTV bucket — refused ✅
- show credit score by broker — refused ✅
- average credit score by region — refused ✅
- show arrears by region — refused ✅
- arrears balance by broker — refused ✅
- show recoveries in period — refused ✅
- indexed value by region — refused ✅

## Known gaps / follow-ups (discovered, not loosened)

These curated questions state the IDEAL behaviour; the system does not yet meet it, so they are xfailed with the reason below rather than having their expectation weakened.

- `kpi_032` **average loan size** — 'size' is parsed as the ticket-size dimension, so this returns balance by ticket bucket instead of a mean-size KPI. Use 'average loan balance'. Follow-up: metric vocabulary for 'loan size'.
- `filt_159` **balance where LTV above 50%** — Bare 'balance where <filter>' (no total/how-much/how-many cue) resolves the metric to LTV and drops the filter. Use 'total balance where LTV above 50%'. Follow-up: treat 'balance where ...' as filtered balance.
- `risk_207` **largest regional concentration** — Resolves to a loan-level ranking TABLE, not a region concentration bar. Use 'which region has the largest balance'. Follow-up: route 'largest <dim> concentration' to a grouped concentration bar.
- `risk_211` **exposure to London** — A place name ('exposure to London') is not turned into a region filter — the whole-book balance is returned. Follow-up: parse place names as region-value filters (or clarify).
- `risk_212` exposure to the South East
- `risk_217` **concentration by LTV bucket** — The bucket field name pulls the MEASURE (WA LTV / mean age) instead of balance, so 'concentration by LTV bucket' returns WA LTV per bucket, not the balance concentration. Use 'balance by LTV bucket'. Follow-up: prefer balance for 'concentration by <bucket>'.
- `risk_218` concentration by age bucket
- `risk_222` **balance by property value band** — 'property value band' is mis-mapped to the age bucket (no valuation-band dimension in the tape). Follow-up: add a valuation-band dimension or refuse.
- `risk_223` **high age borrower exposure** — No age threshold is parsed from 'high age', so the whole-book balance is returned unfiltered. Use 'balance for borrowers over 80'. Follow-up: qualitative-threshold handling or clarify.
- `dq_228` **missing region count** — No dedicated 'missing/excluded count' intent — the system surfaces coverage via the reconciliation block on a normal result, not as a standalone count. Follow-up: a data-quality intent that answers missing/excluded counts directly.
- `dq_229` missing borrower age count
- `dq_230` missing LTV count
- `dq_231` loans excluded from LTV analysis
- `ambig_247` **show best brokers** — Currently answered as balance by a dimension (a subjective/unavailable concept was silently substituted with balance). Ideal: clarify or refuse. Follow-up: detect subjective/unavailable concepts ('best', 'bad', 'interesting', 'profitability').
- `ambig_248` show bad regions
- `ambig_249` show profitability by region
- `ambig_250` show me interesting regions
